import type { IncomingMessage,ServerResponse } from "node:http";import type { BelpexArchive } from "./belpex-history.js";import { selectBelpexRange } from "./belpex-history.js";
export const handleBelpexHistoryRequest=(request:IncomingMessage,response:ServerResponse,archive:BelpexArchive)=>{const url=new URL(request.url??"/",`http://${request.headers.host}`);if(url.pathname!=="/api/history/belpex")return false;response.setHeader("Access-Control-Allow-Origin","*");response.setHeader("Cache-Control","public, max-age=86400");response.setHeader("Content-Type","application/json");if(request.method!=="GET"){response.statusCode=405;response.end(JSON.stringify({error:"method_not_allowed"}));return true;}const result=selectBelpexRange(archive,url.searchParams.get("start")??"",url.searchParams.get("end")??"");if(!result){response.statusCode=400;response.end(JSON.stringify({error:"invalid_range"}));return true;}response.statusCode=200;response.end(JSON.stringify(result));return true;};
