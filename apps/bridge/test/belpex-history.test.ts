import assert from "node:assert/strict";import test from "node:test";import {loadBelpexArchive,selectBelpexRange} from "../src/belpex-history.ts";
const csv="start_utc,resolution_minutes,price_eur_mwh\n2025-09-30T22:00:00Z,60,50.00\n2025-09-30T23:00:00Z,60,60.00\n2025-10-01T00:00:00Z,15,-5.00\n2025-10-01T00:15:00Z,15,0.00\n";
test("laadt een aaneengesloten archief met uur- en kwartierprijzen",()=>{const archive=loadBelpexArchive(csv);assert.equal(archive.points.length,4);assert.equal(archive.points[2]!.priceEurMwh,-5);assert.equal(archive.end,"2025-10-01T00:30:00.000Z");});
test("selecteert alleen het gevraagde bereik en rapporteert dekking",()=>{const archive=loadBelpexArchive(csv);const result=selectBelpexRange(archive,"2025-09-30T22:30:00Z","2025-10-01T00:15:00Z");assert.equal(result?.quality,"complete");assert.equal(result?.points.length,3);assert.equal(selectBelpexRange(archive,"2020-01-01T00:00:00Z","2020-01-02T00:00:00Z")?.quality,"incomplete");});
test("weigert gaten en ongeldige of langere-dan-vijf-jaar ranges",()=>{assert.throws(()=>loadBelpexArchive(csv.replace("2025-09-30T23:00:00Z","2025-09-30T23:15:00Z")),/INCOMPLETE/);const archive=loadBelpexArchive(csv);assert.equal(selectBelpexRange(archive,"bad","2025-10-01T00:00:00Z"),undefined);assert.equal(selectBelpexRange(archive,"2020-01-01T00:00:00Z","2026-01-02T00:00:00Z"),undefined);});
